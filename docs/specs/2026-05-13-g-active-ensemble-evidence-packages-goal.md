# G Active Ensemble Evidence Packages Goal

**Opened:** 2026-05-13
**Status:** complete

## Goal

Supplement the G.1-G.3 missing-evidence package with a small active synthetic
package that exercises every required evidence role for external ensemble
percentile claims.

## First Target

Add a deterministic active package containing external trace, election input,
metric output, diagnostics, BISECT plan, and RCTX context fixtures.

## Acceptance

- [x] Add an active wave and pulse context for G active ensemble evidence.
- [x] Add a hash-bound active G.1-G.3 synthetic evidence package.
- [x] Add `bisect-ensemble` verifier coverage for the docs package.
- [x] Update G.1/G.2/G.3 papers and ledgers to cite the active package without
  claiming real external ensemble percentile findings.
- [x] Close the slice with validation commands and commit evidence.

## Non-Goals

- Do not claim real GerryChain/ReCom ensemble percentile results from synthetic
  fixtures.
- Do not remove the missing-evidence package; it remains the ledger for real
  artifacts still needed before headline empirical claims.
