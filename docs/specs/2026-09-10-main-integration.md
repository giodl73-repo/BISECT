# Main integration record

Integrates research checkpoint `81f396f5` and remote main `e0764d2a` without
rewriting either history. The checkpoint branch remains an exact source
snapshot for its published evidence. Historical/unrelated feature branches
are not implicitly merged.

Repository rules require a linear-history pull request and nine status checks.
The local merge candidate `af7dc5f7` passed the checks below, but its direct
push was rejected by those rules. The identical integration changes are
submitted from `integration/districting-main-2026-09-10` as a single-parent
commit on upstream main; only this explanatory note differs from the tested
merge tree. The original checkpoint remains independently available.

## Overlap resolution

- CLI: retain upstream Windows 8-MiB worker-thread startup and the research
  ceremony command.
- National Tier 1 runner: retain checkpoint lexical `os.path.abspath` path
  handling and adopt upstream `state_config.STATE_CONFIG_2020` import.
- National Tier 2 runner: retain lexical path handling and the same centralized
  configuration import.

All other upstream changes, including configuration consolidation, media
work, startup/build cleanup and superseded-evidence retention policy, remain
part of the merge. Only Tier 1 required manual conflict resolution; its large
text conflict was caused by line-ending differences around the one-line import.

## Frozen evidence is not regenerated by a merge

The import cleanup changes the national runners' source hashes. Their frozen
Tier 1/Tier 2 reports still name checkpoint-era hashes; verification directly
against the merged source will correctly reject that mismatch. Do not edit the
reports to pretend they were generated by the merged code. For exact historical
verification, use checkpoint `81f396f5`, its data-vault mounts, and its recorded
replay instructions. Any new national regeneration must be a new evidence run.

The new county-search diagnostic engines themselves are unchanged by this
merge. The historical readiness audit also records the earlier edge-weight
source; upstream warning cleanup changes that source hash without retroactively
changing the recorded audit. No full national rerun is claimed for this merge.

The [resume checkpoint](2026-09-10-research-resume-checkpoint.md) remains the
research handoff. Latest county-search findings still await manuscript-wide
integration and full-plan/independent validation.

## Merge checks

- 35 distinct county/RI diagnostic unit tests passed (two discovery patterns
  overlap on four tests).
- Three new runner-integration regressions and 11 centralized-config tests passed.
- All 104 ILP unit tests and both hostile-corpus integration tests passed.
- Both ceremony CLI tests passed; the rebuilt Windows executable starts and
  prints ceremony help successfully.
- The ceremony fixture regenerated and its timestamp-tamper case was rejected.

These checks cover the overlaps and research additions, not every unrelated
upstream subsystem. No nationwide geometry regeneration was performed for this merge.
