# Fork Context: Pulse 01

Execute the M.1 LODES economic evidence slice end to end.

## Decision

This slice needs a small helper extraction plus fixture/docs. The consumer is
Rust verifier coverage, so no standalone CLI verifier is required.
