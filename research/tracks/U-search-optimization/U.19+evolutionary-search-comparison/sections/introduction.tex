\section{Introduction}

U.19 documents \texttt{bisect-pareto} and \texttt{bisect-cli::pareto\_cmd},
exposed through \texttt{bisect pareto} and selected-frontier audit flags. The paper is an
evolutionary/multi-objective implementation paper, not a proof that a computed
frontier is globally complete.
