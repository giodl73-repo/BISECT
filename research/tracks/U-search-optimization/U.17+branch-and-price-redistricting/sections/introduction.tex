\section{Introduction}

U.17 documents \texttt{bisect-column} and \texttt{bisect-cli::exact\_cmd},
exposed as \texttt{bisect exact --method branch-and-price}. The paper is an exact-optimization
implementation paper about column-generation contracts, not a claim that the
large-instance pricing strategy is complete.
