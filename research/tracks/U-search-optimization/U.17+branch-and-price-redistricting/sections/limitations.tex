\section{Limitations}

The current branch-and-price story is intentionally fixture-scale. Branching
strategy, large-instance pricing, and broad runtime claims remain staged. The
paper should not describe a formulation-only report or partial column pool as a
solved exact instance.

The paper also does not claim that the generated column universe is complete
unless the solver report and fixture contract say so. Pricing failures,
duplicate columns, and disconnected candidates must remain visible because they
are part of the implementation evidence.

The path4 fixture is useful because the exact-cover decision is inspectable by
hand, but that same smallness limits the claim. A future performance paper
needs larger formulation-only examples, solver versions, hardware, full logs,
pricing-round transcripts, and archived output directories before comparing
branch-and-price with branch-and-cut or construction heuristics.
