\section{Introduction}

U.20 documents `rplan-core`, `rplan-io`, `rplan-audit`, `bisect verify`, and
`rplan-cli`. The paper asks whether BISECT can use a neutral
plan/context/certificate family as the fixed point for construction, exact,
local-search, Pareto, and future algorithm families.
