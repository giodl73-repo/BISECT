\section{Method}

The staged method approximates a Fiedler vector, sorts units by the spectral
coordinate, and evaluates population-balanced sweep cuts. Recursive calls pass
proportional target fractions so odd and non-power-of-two district counts are
handled without silently changing the intended seat allocation.
