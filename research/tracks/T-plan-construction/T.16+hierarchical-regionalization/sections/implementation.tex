\section{Implementation}

The implementation lives in \texttt{bisect-clustering::regionalization} and is
exposed through \texttt{bisect state --structure regionalization}. Outputs
include regionalization summaries, merge logs, parameter hashes, and algorithm
lineage.
